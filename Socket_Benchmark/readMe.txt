Compile and run the Java server program in one terminal:

Copy code
javac JavaServer.java
java JavaServer
Compile and run the C++ client program in another terminal:

Copy code
g++ client.cpp -o client -lws2_32
./client.exe